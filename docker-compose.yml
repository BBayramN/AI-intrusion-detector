services:
  web:
    build:
      context: .
    ports:
      - "8000:8000"
    network_mode: host
    cap_add:
      - NET_ADMIN
      - NET_RAW
    volumes:
      - /home/deployuser/data:/app/data/  # Mount host directory to container directory
